<table>
  <thead>
    <tr>
      <th>ジャンル一覧</th>
    </tr>
  </thead>
  
  <tbody>
    <% genres.each do |genre| %>
      <tr>
        <td><%= link_to genre.name, genre_path(genre.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>